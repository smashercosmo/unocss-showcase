import { Box } from "@/components/Box/Box";

export function App() {
  return (
    <Box inlineSize={300}>
      <h1>
        Unocss Showcase. Run `npm storybook` to play around with components.
      </h1>
    </Box>
  );
}
